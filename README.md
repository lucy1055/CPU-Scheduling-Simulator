# CPU-Scheduling-Simulator
Simulates core CPU scheduling algorithms (e.g., FCFS, SJF, Round Robin) to analyze process execution order, waiting/turnaround times, and scheduling efficiency based on underlying OS scheduling principles
